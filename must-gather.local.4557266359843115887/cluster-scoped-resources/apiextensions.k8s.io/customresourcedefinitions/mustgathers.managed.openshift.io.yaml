---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.3.0
    operatorframework.io/installed-alongside-dcb9e5e518c9e49b: openshift-must-gather-operator/must-gather-operator.v0.1.158-adcbf04
  creationTimestamp: "2022-05-24T18:27:18Z"
  generation: 1
  labels:
    operators.coreos.com/must-gather-operator.openshift-must-gather-operator: ""
  name: mustgathers.managed.openshift.io
  resourceVersion: "37037"
  uid: d311a2f5-2c0c-4196-a3a3-c2e9ccceaca4
spec:
  conversion:
    strategy: None
  group: managed.openshift.io
  names:
    kind: MustGather
    listKind: MustGatherList
    plural: mustgathers
    singular: mustgather
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MustGather is the Schema for the mustgathers API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: MustGatherSpec defines the desired state of MustGather
            properties:
              caseID:
                description: The is of the case this must gather will be uploaded
                  to
                type: string
              caseManagementAccountSecretRef:
                description: the secret container a username and password field to
                  be used to authenticate with red hat case management systems
                properties:
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      TODO: Add other useful fields. apiVersion, kind, uid?'
                    type: string
                type: object
              internalUser:
                default: true
                description: A flag to specify if the upload user provided in the  caseManagementAccountSecret
                  is a RH internal user. See documentation for further information.
                type: boolean
              mustGatherImages:
                description: 'The list of must gather images to run, optional, it
                  will default to: $DEFAULT_MUST_GATHER_IMAGE'
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
              mustGatherTimeout:
                description: 'A time limit for gather command to complete a floating
                  point number with an optional suffix: "s" for seconds (the default),
                  "m" for minutes, "h" for hours or "d" for days will default to:
                  $DEFAULT_MUST_GATHER_TIMEOUT'''
                type: string
              proxyConfig:
                description: This represents the proxy configuration to be used. If
                  left empty it will default to the cluster-level proxy configuration.
                properties:
                  httpProxy:
                    description: httpProxy is the URL of the proxy for HTTP requests.  Empty
                      means unset and will not result in an env var.
                    type: string
                  httpsProxy:
                    description: httpsProxy is the URL of the proxy for HTTPS requests.  Empty
                      means unset and will not result in an env var.
                    type: string
                  noProxy:
                    description: noProxy is the list of domains for which the proxy
                      should not be used.  Empty means unset and will not result in
                      an env var.
                    type: string
                type: object
              serviceAccountRef:
                description: the service account to use to run the must gather job
                  pod, defaults to default  +kubebuilder:default:="{Name:default}"
                properties:
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      TODO: Add other useful fields. apiVersion, kind, uid?'
                    type: string
                type: object
            required:
            - caseID
            - caseManagementAccountSecretRef
            type: object
          status:
            description: MustGatherStatus defines the observed state of MustGather
            properties:
              completed:
                type: boolean
              lastUpdate:
                format: date-time
                type: string
              reason:
                type: string
              status:
                enum:
                - Success
                - Failure
                type: string
            required:
            - completed
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: MustGather
    listKind: MustGatherList
    plural: mustgathers
    singular: mustgather
  conditions:
  - lastTransitionTime: "2022-05-24T18:27:18Z"
    message: no conflicts found
    reason: NoConflicts
    status: "True"
    type: NamesAccepted
  - lastTransitionTime: "2022-05-24T18:27:18Z"
    message: the initial names have been accepted
    reason: InitialNamesAccepted
    status: "True"
    type: Established
  storedVersions:
  - v1alpha1
