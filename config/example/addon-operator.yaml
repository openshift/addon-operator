apiVersion: addons.managed.openshift.io/v1alpha1
kind: AddonOperator
metadata:
  name: addon-operator
spec:
  ocm:
    endpoint: http://api-mock.api-mock.svc.cluster.local
    secret:
      name: pull-secret
      namespace: api-mock
